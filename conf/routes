# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index
POST    /sa                         controllers.SaQuestionController.postQuestion
GET     /sa                         controllers.SaQuestionController.getAllQuestion
#GET     /sa/:id                     controllers.SaQuestionController.getQuestion(id: Long)
POST    /sa/answer                  controllers.SaQuestionController.postAnswer
GET     /sa/answer/:id              controllers.SaQuestionController.getAnswer(id: Long)

GET     /reset                      controllers.AdminController.reset
POST    /reset                      controllers.AdminController.reset

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
